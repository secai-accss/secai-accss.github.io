source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"   # keep only if you use `remote_theme:` in _config.yml
gem "webrick", "~> 1.8"     # needed for Ruby 3.x dev server
